{
  "name": "parser-generator-visualizer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "cd client && npm install",
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev"
  }
}
